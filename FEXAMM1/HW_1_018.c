#include<stdio.h>
int main(){
    for(int i = 9; i>=5 ; i--){ //สร้างบรรทัดถบรรทัดโดยใช้เลข9-5(5จำนวน)
        for(int j = 5; j<=i ; j++){//สร้างตัวเลขเริ่มจะ5เเล้วเพิ่มขึ้นไปถึง9เเล้วลดลงมาทีละ1
            printf("%d",j);
        }
        printf("\n");
    }

}